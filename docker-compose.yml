# Main application Compose file
version: "3.8"

networks:
  signoz-net:
    driver: bridge
    name: signoz-net
    # Allow our services to connect to an external SigNoz instance
    external: true

services:
  # --- Product Service (Your Application) ---
  product-service:
    build:
      context: .
      dockerfile: ./product-service/Dockerfile
    container_name: product-service
    ports:
      - "8082:8082"
    networks:
      - signoz-net
    env_file:
      - .env # OTEL_EXPORTER_OTLP_ENDPOINT should point to external SigNoz instance
    volumes:
      - ./product-service/data.json:/app/data.json:ro
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8082/api/v1/healthz"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          cpus: '0.25'
          memory: 128M

  # --- Product Simulator Service (Load Generator) ---
  product-simulator:
    build:
      context: ./tests
      dockerfile: Dockerfile
    depends_on:
      product-service:
        condition: service_healthy # Wait for product-service to be healthy
    env_file:
      - .env
    networks:
      - signoz-net
    deploy:
      replicas: 5

  